import Section from './section';

// VPNのメンバー一覧
const Member = (): JSX.Element => (
  // eslint-disable-next-line react/no-children-prop
  <Section title="Member" children={undefined} />
);

export default Member;
