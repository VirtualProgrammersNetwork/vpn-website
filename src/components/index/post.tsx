import Section from './section';

// VPNに投稿された記事一覧
const Post = (): JSX.Element => (
  // eslint-disable-next-line react/no-children-prop
  <Section title="Post" children={undefined} />
);

export default Post;
